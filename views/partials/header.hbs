<div class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" data-toggle="collapse" data-target=".navbar-collapse" class="navbar-toggle"><span class="sr-only">Toggle navigation</span><span class="icon-bar"></span><span class="icon-bar"></span><span class="icon-bar"></span></button><a href="/" class="navbar-brand"><span class="fa fa-cube"></span>Custos IMS</a>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
          {{#if user}}
            <li><a href="/inventory">Inventory Search</a></li>
          {{/if}}
          {{#if user.isSiteAdmin}}
               <li><a href="/usermanagement">User Management</a></li>
               <li><a href="/admin">System Administration</a></li>
               <li class="dropdown">
                   <a href="#" data-toggle="dropdown" class="dropdown-toggle" role="button">Treasury</a>
                   <i class="caret"></i>
                       <ul class="dropdown-menu">
                           <li><a href="/purchaseorder">Create Purchase Order</a></li>
                           <li class="divider"></li>
                           <li><a href="/reimbursement">Create Reimbursement Form</a></li>
                       </ul>
                </li>
          {{/if}}
      </ul>
      <ul class="nav navbar-nav navbar-right">
        {{#unless user}}
        <li><a href="/login">Login</a></li>
        {{else}}
        <li class="dropdown">
          <a href="#" data-toggle="dropdown" class="dropdown-toggle" role="button">
            {{#if user.profile.picture}}
            <img src="{{user.profile.picture}}"/>
            {{else}}
            <img src="{{getGravatarURL user 60}}"/>
            {{/if}}
            {{#if user.profile.name}}
              {{user.profile.name}}&nbsp;
            {{else}} 
              {{#if user.email}}
                {{user.email}}&nbsp;
              {{else}}
                {{user.id}}&nbsp;
              {{/if}}
            {{/if}}
            <i class="caret"></i>
          </a>
          <ul class="dropdown-menu">
            <li><a href="/account">My Account</a></li>
              <li class="divider"></li>
              {{#if user.isSiteOwner}}
              <li><a href="/transferOwnership">Transfer Site Ownership</a></li>
              <li class="divider"></li>
              {{/if}}
            <li><a href="/logout">Logout</a></li>
          </ul>
        </li>
        {{/unless}}
      </ul>
    </div>
  </div>
</div>

{{! user.profile.picture || user.email || user.id}}